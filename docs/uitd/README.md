# UITD Models

- `topracing-flow.uitd`: complete navigation model, from public entry to protected flows.

The file keeps multiple fragments, but all of them now live inside one UITD so the system can be read as a single model.
